---
"branch-context-vscode": minor
---

Show Branch AI Sessions in workspaces without Branch Context initialized, keep active session detection scoped to the current workspace, and avoid duplicate refreshes after the extension writes agent session metadata.
