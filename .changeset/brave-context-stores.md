---
"branch-context": minor
"branch-context-vscode": minor
---

Simplify context storage configuration with direct `branches_folder` and `templates_folder` paths, infer templates from branch prefixes, and add a CLI command for changing the templates folder.
