---
"branch-context-vscode": minor
---

Replace the current context and status bar UI with a Config view that shows mode, branch, base, template, CLI info, and issues, plus actions for sync, backup, config, branch checkout, template apply, and opening global storage.
